(()=>{
'use strict';
const FED='52cfa65e-9782-4a81-ab35-e2f981fcb7a9';
const ARGON='a4a2e2fa-0635-46a5-8969-1d0fef40444f';
const API='https://api.foys.io/competition/public-api/v1';
const STORE='basketballApp.selection.v3';
const cache=new Map(),orgTeamCache=new Map(),logoCache=new Map();
let argonTeams=[],pools=[],loading=false,renderToken=0,filterGuid='',currentRows=[];
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
const d=v=>String(v||'').slice(0,10),t=v=>String(v||'').slice(0,5);
const headers=()=>({Accept:'application/json','X-FederationID':FED});
function season(){const n=new Date(),y=n.getMonth()>=6?n.getFullYear():n.getFullYear()-1;return{start:`${y}-07-01`,end:`${y+1}-06-30`}}
function ordinal(n){return n?`${n}e`:'—'}
function selection(){try{return JSON.parse(localStorage.getItem(STORE)||'null')}catch{return null}}
function teamGuid(m,side){return String(m?.[`${side}TeamGuid`]||m?.[`${side}Team`]?.guid||'')}
function teamName(m,side){const sponsor=m?.[`${side}TeamSponsorClubName`],org=m?.[`${side}Organisation`]?.name,tn=m?.[`${side}TeamName`]||m?.[`${side}Team`]?.name||'';return [sponsor||org,tn].filter(Boolean).join(' ').trim()||'Onbekend team'}
function competitionKey(m){const c=m?.competition||{};return String(c.guid||c.id||c.competitionGuid||c.name||'').trim()||'__none__'}
function competitionName(m){return m?.competition?.name||'Competitie'}
function hasScore(m){return m?.homeScore!==null&&m?.homeScore!==undefined&&m?.awayScore!==null&&m?.awayScore!==undefined}
function isPlayed(m){return hasScore(m)&&(m.status==='Final'||m.status==='Played'||d(m.date)<=new Date().toISOString().slice(0,10))}
function formatDay(v){const x=new Date(`${d(v)}T12:00:00`);return new Intl.DateTimeFormat('nl-NL',{weekday:'long',day:'2-digit',month:'long',year:'numeric'}).format(x).toUpperCase()}
async function json(url){const r=await fetch(url,{headers:headers(),cache:'no-store'});if(!r.ok)throw new Error(`${r.status} ${r.statusText}`);return r.json()}
async function loadArgonTeams(){if(argonTeams.length)return argonTeams;const rows=await json(`${API}/organisations/${ARGON}/teams`);argonTeams=(Array.isArray(rows)?rows:[]).filter(x=>x?.guid&&!String(x.guid).startsWith('all-'));for(const tm of argonTeams)if(tm.logoUrl)logoCache.set(String(tm.guid),tm.logoUrl);return argonTeams}
async function teamMatches(guid){if(cache.has(guid))return cache.get(guid);const q=season(),all=[];let skip=0,total=Infinity;while(skip<total){const p=new URLSearchParams({startDate:q.start,endDate:q.end,teamGuid:guid,skipCount:String(skip),maxResultCount:'100',sorting:'date asc, startTime asc'}),j=await json(`${API}/matches?${p}`),rows=Array.isArray(j?.items)?j.items:[];all.push(...rows);total=Number.isFinite(Number(j?.totalCount))?Number(j.totalCount):rows.length;skip+=rows.length;if(!rows.length||rows.length<100)break}cache.set(guid,all);return all}
async function mapLimit(items,limit,fn){let i=0;const out=new Array(items.length),workers=Array.from({length:Math.min(limit,Math.max(1,items.length))},async()=>{while(i<items.length){const n=i++;out[n]=await fn(items[n],n)}});await Promise.all(workers);return out}
function primaryGroup(matches,anchor){const groups=new Map();for(const m of matches){const h=teamGuid(m,'home'),a=teamGuid(m,'away');if(h!==anchor&&a!==anchor)continue;const name=competitionName(m);if(/beker|cup/i.test(name))continue;const k=competitionKey(m);if(!groups.has(k))groups.set(k,[]);groups.get(k).push(m)}if(!groups.size){for(const m of matches){const k=competitionKey(m);if(!groups.has(k))groups.set(k,[]);groups.get(k).push(m)}}return [...groups.entries()].sort((a,b)=>b[1].length-a[1].length)[0]||['__none__',[]]}
function poolMembers(anchor,rows){const set=new Set([anchor]);for(const m of rows){const h=teamGuid(m,'home'),a=teamGuid(m,'away');if(h)set.add(h);if(a)set.add(a)}return set}
function labelsForPool(p){const map=new Map();for(const m of p.matches){const h=teamGuid(m,'home'),a=teamGuid(m,'away');if(h&&!map.has(h))map.set(h,teamName(m,'home'));if(a&&!map.has(a))map.set(a,teamName(m,'away'))}for(const g of p.anchorGuids){const tm=argonTeams.find(x=>String(x.guid)===String(g));if(tm&&!map.has(g))map.set(g,`SV Argon ${tm.name}`)}return map}
function standings(p){const stats=new Map([...p.members].map(g=>[g,{guid:g,played:0,points:0,for:0,against:0}]));for(const m of p.matches.filter(isPlayed)){const h=stats.get(teamGuid(m,'home')),a=stats.get(teamGuid(m,'away'));if(!h||!a)continue;const hs=Number(m.homeScore),as=Number(m.awayScore);h.played++;a.played++;h.for+=hs;h.against+=as;a.for+=as;a.against+=hs;if(hs>as)h.points+=2;else if(as>hs)a.points+=2;else{h.points++;a.points++}}const arr=[...stats.values()];arr.sort((x,y)=>y.points-x.points||((y.for-y.against)-(x.for-x.against))||y.for-x.for||String(x.guid).localeCompare(String(y.guid)));return arr}
function directLogo(m,side){return m?.[`${side}TeamLogoUrl`]||m?.[`${side}Team`]?.logoUrl||m?.[`${side}Organisation`]?.logoUrl||m?.[`${side}TeamSponsorClubLogoUrl`]||m?.[`${side}ClubLogoUrl`]||m?.[`${side}LogoUrl`]||''}
function organisationId(m,side){return String(m?.[`${side}Organisation`]?.id||m?.[`${side}OrganisationId`]||m?.[`${side}ClubId`]||'')}
async function organisationTeams(orgId){if(!orgId)return[];if(orgTeamCache.has(orgId))return orgTeamCache.get(orgId);try{const rows=await json(`${API}/organisations/${encodeURIComponent(orgId)}/teams`),list=Array.isArray(rows)?rows:[];orgTeamCache.set(orgId,list);return list}catch{orgTeamCache.set(orgId,[]);return[]}}
async function resolvePoolLogos(p){const reps=new Map();for(const m of p.matches){for(const side of ['home','away']){const g=teamGuid(m,side);if(g&&!reps.has(g))reps.set(g,{m,side})}}await mapLimit([...p.members],5,async g=>{if(logoCache.has(g))return;const rep=reps.get(g);if(!rep)return;const direct=directLogo(rep.m,rep.side);if(direct){logoCache.set(g,direct);return}const org=organisationId(rep.m,rep.side);if(!org)return;const list=await organisationTeams(org),tm=list.find(x=>String(x.guid)===String(g));if(tm?.logoUrl)logoCache.set(g,tm.logoUrl)})}
async function buildPools(anchorGuids){const source=await mapLimit(anchorGuids,5,async g=>({g,rows:await teamMatches(g)})),raw=[];for(const x of source){const [competition,leagueRows]=primaryGroup(x.rows,x.g),members=poolMembers(x.g,leagueRows);if(members.size<2)continue;raw.push({anchorGuids:[x.g],competition,competitionName:competitionName(leagueRows[0]),members})}const bySig=new Map();for(const p of raw){const sig=`${p.competition}|${[...p.members].sort().join(',')}`;if(bySig.has(sig))bySig.get(sig).anchorGuids.push(...p.anchorGuids);else bySig.set(sig,p)}const result=[...bySig.values()],need=[...new Set(result.flatMap(p=>[...p.members]))];await mapLimit(need,6,g=>teamMatches(g));for(const p of result){const byId=new Map();for(const g of p.members){for(const m of cache.get(g)||[]){if(competitionKey(m)!==p.competition)continue;const h=teamGuid(m,'home'),a=teamGuid(m,'away');if(p.members.has(h)&&p.members.has(a))byId.set(String(m.id),m)}}p.matches=[...byId.values()];p.standings=standings(p);p.position=new Map(p.standings.map((x,i)=>[x.guid,i+1]));p.labelMap=labelsForPool(p);await resolvePoolLogos(p)}return result}
function poolForAnchor(guid){return pools.find(p=>p.anchorGuids.includes(guid))||null}
function inject(){if($('view-feed'))return;const sec=document.createElement('section');sec.id='view-feed';sec.className='view';sec.innerHTML='<div class="section-head"><div><h1>Feed</h1><p id="feedSubtitle">Uitslagen uit de poules waarin SV Argon speelt.</p></div></div><div id="feedSummary"></div><div id="feedFilter"></div><div id="feedRoot"><div class="empty">Feed laden…</div></div>';$('view-tasks')?.insertAdjacentElement('afterend',sec);const nav=document.querySelector('.tabs'),cal=nav?.querySelector('.tab[data-view="calendar"]');if(nav&&!nav.querySelector('.tab[data-view="feed"]')){const b=document.createElement('button');b.className='tab';b.type='button';b.dataset.view='feed';b.innerHTML='<span class="tab-icon">▤</span>Feed';b.onclick=async()=>{document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v===sec));document.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x===b));await render(true)};if(cal)nav.insertBefore(b,cal);else nav.appendChild(b)}}
function logoHtml(guid,cls='feed-team-logo'){const src=logoCache.get(String(guid));return src?`<img class="${cls}" src="${esc(src)}" alt="" loading="lazy">`:''}
function summaryHtml(anchors){const rows=[];for(const g of anchors){const p=poolForAnchor(g),tm=argonTeams.find(x=>String(x.guid)===String(g)),pos=p?.position.get(g);if(tm&&p&&pos)rows.push(`<div class="feed-standing-item">${logoHtml(g,'feed-standing-logo')}<span class="feed-standing-name">SV Argon ${esc(tm.name)}</span><span class="feed-pos">${esc(ordinal(pos))}</span><span class="feed-of">van ${p.members.size}</span></div>`)}return rows.length?`<div class="feed-standing-grid">${rows.join('')}</div>`:''}
function teamRow(guid,name,pos){return `<div class="feed-team-row">${logoHtml(guid)}<span class="feed-team-name">${esc(name)}</span>${pos?`<span class="feed-pos">${esc(ordinal(pos))}</span>`:''}</div>`}
function card(m,p){const h=teamGuid(m,'home'),a=teamGuid(m,'away'),hp=p.position.get(h),ap=p.position.get(a),home=p.labelMap.get(h)||teamName(m,'home'),away=p.labelMap.get(a)||teamName(m,'away'),score=`${m.homeScore} – ${m.awayScore}`,loc=[m.accommodationName,m.fieldName].filter(Boolean).join(' · ');return `<article class="event feed-event" data-feed-match="${esc(m.id)}"><div class="event-row"><div class="time-col"><div class="event-time score">${esc(score)}</div><span class="event-status">Eindstand</span><span class="feed-start">Start ${esc(t(m.startTime))}</span></div><div class="event-main"><span class="badge badge-final">Uitslag</span><div class="feed-matchup">${teamRow(h,home,hp)}<div class="feed-versus">—</div>${teamRow(a,away,ap)}</div><div class="meta">${esc(loc||'Locatie niet bekend')}</div><div class="competition">${esc(p.competitionName)} · wedstrijd-ID ${esc(m.id)}</div></div></div></article>`}
function renderRows(rows){if(!rows.length)return '<div class="empty">Nog geen gespeelde wedstrijden gevonden voor deze selectie.</div>';const byDate=new Map();for(const x of rows){const key=d(x.m.date);if(!byDate.has(key))byDate.set(key,[]);byDate.get(key).push(x)}return [...byDate.entries()].sort((a,b)=>b[0].localeCompare(a[0])).map(([date,list])=>`<section class="day"><div class="day-head">${esc(formatDay(date))}</div>${list.sort((x,y)=>t(y.m.startTime).localeCompare(t(x.m.startTime))).map(x=>card(x.m,x.p)).join('')}</section>`).join('')}
function drawRows(){const root=$('feedRoot');if(!root)return;const rows=filterGuid?currentRows.filter(x=>teamGuid(x.m,'home')===filterGuid||teamGuid(x.m,'away')===filterGuid):currentRows;root.innerHTML=renderRows(rows)}
function renderFilter(pool){const host=$('feedFilter');if(!host)return;if(!pool){host.innerHTML='';filterGuid='';return}if(filterGuid&&!pool.members.has(filterGuid))filterGuid='';const options=[...pool.members].sort((a,b)=>(pool.position.get(a)||999)-(pool.position.get(b)||999)).map(g=>`<option value="${esc(g)}" ${filterGuid===g?'selected':''}>${esc(pool.labelMap.get(g)||g)}${pool.position.get(g)?` · ${esc(ordinal(pool.position.get(g)))}`:''}</option>`).join('');host.innerHTML=`<div class="feed-filter-card"><label for="feedTeamFilter">Team in deze poule</label><select id="feedTeamFilter"><option value="">Alle teams in deze poule</option>${options}</select></div>`;$('feedTeamFilter').onchange=e=>{filterGuid=e.target.value;drawRows()}}
async function render(force=false){inject();if(loading&&!force)return;const token=++renderToken,root=$('feedRoot');if(!root)return;loading=true;root.innerHTML='<div class="empty">Poule en uitslagen laden…</div>';try{await loadArgonTeams();const s=selection(),selected=s?.teamGuid||'',anchors=String(selected).startsWith('all-')||!argonTeams.some(x=>String(x.guid)===String(selected))?argonTeams.map(x=>String(x.guid)):[String(selected)];pools=await buildPools(anchors);if(token!==renderToken)return;const one=anchors.length===1?poolForAnchor(anchors[0]):null,tm=anchors.length===1?argonTeams.find(x=>String(x.guid)===String(anchors[0])):null,pos=one?.position.get(anchors[0]);const sub=$('feedSubtitle');if(sub)sub.textContent=tm&&one&&pos?`Alle gespeelde wedstrijden uit de poule van SV Argon ${tm.name} (${ordinal(pos)} van ${one.members.size}).`:'Alle gespeelde wedstrijden uit alle poules waarin een SV Argon-team speelt.';const sum=$('feedSummary');if(sum)sum.innerHTML=summaryHtml(anchors);renderFilter(one);let rows=[];for(const p of pools)for(const m of p.matches)if(isPlayed(m))rows.push({m,p});const dedup=new Map();for(const x of rows)dedup.set(String(x.m.id),x);currentRows=[...dedup.values()].sort((x,y)=>`${d(y.m.date)}T${t(y.m.startTime)}`.localeCompare(`${d(x.m.date)}T${t(x.m.startTime)}`));drawRows()}catch(e){console.error(e);root.innerHTML=`<div class="empty error-box">Feed kon niet worden geladen: ${esc(e.message)}</div>`}finally{loading=false}}
function css(){if($('feedStyle'))return;const s=document.createElement('style');s.id='feedStyle';s.textContent='.feed-standing-grid{display:grid;gap:7px;margin-bottom:12px}.feed-standing-item{display:flex;align-items:center;gap:8px;background:#fff;border:1px solid var(--line);border-radius:12px;padding:9px 11px}.feed-standing-logo,.feed-team-logo{width:24px;height:24px;object-fit:contain;border-radius:5px;background:#fff;flex:0 0 auto}.feed-standing-name{font-size:10px;font-weight:900;min-width:0;flex:1}.feed-of{font-size:8px;color:var(--muted);white-space:nowrap}.feed-pos{display:inline-flex;align-items:center;justify-content:center;min-width:30px;height:22px;padding:0 7px;border-radius:999px;background:#fff0e8;color:var(--orange);font-size:9px;font-weight:950;white-space:nowrap}.feed-filter-card{background:#fff;border:1px solid var(--line);border-radius:12px;padding:10px 11px;margin-bottom:12px}.feed-filter-card label{display:block;font-size:9px;font-weight:850;color:var(--muted);margin-bottom:5px}.feed-filter-card select{width:100%;border:1px solid #d8dae2;border-radius:9px;background:#fff;padding:9px 10px;color:var(--text);font-size:10px;font-weight:800}.feed-start{display:block;margin-top:4px;font-size:8px;font-weight:850;color:var(--muted)}.feed-matchup{display:grid;gap:3px;margin-top:6px}.feed-team-row{display:flex;align-items:center;gap:7px;min-width:0}.feed-team-name{font-size:11px;font-weight:900;line-height:1.25;min-width:0}.feed-versus{display:none}.feed-event .meta{margin-top:7px}.feed-event .event-main{min-width:0}@media(min-width:620px){.feed-team-name{font-size:12px}}';document.head.appendChild(s)}
document.addEventListener('DOMContentLoaded',()=>{css();inject();document.getElementById('teamSelect')?.addEventListener('change',()=>{cache.clear();pools=[];filterGuid='';currentRows=[];setTimeout(()=>{if(document.getElementById('view-feed')?.classList.contains('active'))render(true)},450)});new MutationObserver(()=>{if(document.getElementById('view-feed')?.classList.contains('active'))render()}).observe(document.documentElement,{subtree:true,attributes:true,attributeFilter:['class']})});
})();