(()=>{
'use strict';
const U='https://elpnfmlrkoemjrnzaeok.supabase.co';
const K='sb_publishable_GPzLwaKeevg3e8CNjw9oAQ_50NW2xlg';
const FED='52cfa65e-9782-4a81-ab35-e2f981fcb7a9';
const ARGON='a4a2e2fa-0635-46a5-8969-1d0fef40444f';
const API='https://api.foys.io/competition/public-api/v1';
const STORE='basketballApp.selection.v3';
let sb=null,contextCache=null,contextAt=0,presenceBusy=false,presenceQueued=false,headerBusy=false;
const matchCache=new Map();
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
const d=v=>String(v||'').slice(0,10),t=v=>String(v||'').slice(0,5);
function season(){const n=new Date(),y=n.getMonth()>=6?n.getFullYear():n.getFullYear()-1;return{start:`${y}-07-01`,end:`${y+1}-06-30`}}
function selection(){try{return JSON.parse(localStorage.getItem(STORE)||'null')}catch{return null}}
function label(m,side){return [m?.[`${side}TeamSponsorClubName`]||m?.[`${side}Organisation`]?.name,m?.[`${side}TeamName`]||m?.[`${side}Team`]?.name].filter(Boolean).join(' ').trim()||'Onbekend team'}
function guid(m,side){return String(m?.[`${side}TeamGuid`]||m?.[`${side}Team`]?.guid||'')}
function shortDate(v){try{return new Intl.DateTimeFormat('nl-NL',{weekday:'short',day:'2-digit',month:'short',year:'numeric'}).format(new Date(`${d(v)}T12:00:00`))}catch{return d(v)}}
function toast(msg){const el=$('toast');if(!el)return alert(msg);el.textContent=msg;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2400)}
async function client(){if(sb)return sb;const mod=await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm');sb=mod.createClient(U,K,{auth:{persistSession:true,detectSessionInUrl:true,autoRefreshToken:true}});sb.auth.onAuthStateChange(()=>{contextCache=null;contextAt=0;matchCache.clear();schedulePresence(true);setTimeout(applyHeaderLogo,120)});return sb}
async function context(force=false){if(!force&&contextCache&&Date.now()-contextAt<10000)return contextCache;const s=await client(),{data:{session}}=await s.auth.getSession();if(!session){contextCache=null;contextAt=Date.now();return null}const {data:m,error}=await s.rpc('sync_current_member');if(error||!m?.active){contextCache=null;contextAt=Date.now();return null}const {data:rows,error:te}=await s.from('member_teams').select('team_id,teams(id,team_name,foy_team_guid)').eq('member_id',m.id);if(te)throw te;contextCache={member:m,memberTeams:(rows||[]).map(x=>x.teams?{...x.teams,id:x.team_id}:null).filter(Boolean)};contextAt=Date.now();return contextCache}
async function teamMatches(teamGuid){if(matchCache.has(teamGuid))return matchCache.get(teamGuid);const q=season(),all=[];let skip=0,total=Infinity;while(skip<total){const p=new URLSearchParams({startDate:q.start,endDate:q.end,teamGuid,skipCount:String(skip),maxResultCount:'100',sorting:'date asc, startTime asc'}),r=await fetch(`${API}/matches?${p}`,{headers:{Accept:'application/json','X-FederationID':FED},cache:'no-store'});if(!r.ok)break;const j=await r.json(),rows=Array.isArray(j?.items)?j.items:[];all.push(...rows);total=Number(j?.totalCount)||rows.length;skip+=rows.length;if(!rows.length||rows.length<100)break}matchCache.set(teamGuid,all);return all}
function fixPlayerCounts(){for(const card of document.querySelectorAll('#agendaList article.event[data-match-id],#gamesList article.event[data-match-id]')){const meta=card.querySelector('.meta'),pc=card.querySelector('.player-count');if(!meta||!pc)continue;const br=meta.querySelector('br');if(pc.previousElementSibling?.classList.contains('player-sep')&&(!br||pc.nextSibling===br))continue;meta.querySelectorAll('.player-sep').forEach(x=>x.remove());pc.remove();const sep=document.createElement('span');sep.className='player-sep';sep.textContent=' · ';if(br){meta.insertBefore(sep,br);meta.insertBefore(pc,br)}else{meta.appendChild(sep);meta.appendChild(pc)}pc.classList.add('player-count-inline-final')}}
function injectCss(){if($('finalFixCss'))return;const s=document.createElement('style');s.id='finalFixCss';s.textContent='.brand-mark.has-team-logo{background:#fff!important;padding:3px!important;overflow:hidden}.brand-mark.has-team-logo img{display:block;width:100%;height:100%;object-fit:contain;border-radius:7px}.player-count-inline-final{display:inline!important;margin:0!important;font-size:inherit!important;line-height:inherit!important;font-weight:850!important;color:var(--navy)!important}.player-sep{color:var(--muted)}.detail-task-role::after{content:": ";}.detail-task-role{margin-right:3px}.personal-presence-card .attendance-choice{margin-top:8px}.personal-presence-card .mini-button.selected{box-shadow:0 0 0 2px var(--navy) inset}';document.head.appendChild(s)}
async function applyHeaderLogo(){if(headerBusy)return;const mark=document.querySelector('.brand-mark');if(!mark)return;headerBusy=true;try{let src='';const s=selection();if(s&&!String(s.teamGuid||'').startsWith('all-'))src=s.teamLogo||'';if(!src)src=s?.clubLogo||'';try{const r=await fetch(`${API}/organisations/${ARGON}/teams`,{headers:{Accept:'application/json','X-FederationID':FED},cache:'no-store'});if(r.ok){const rows=(await r.json())||[],real=(Array.isArray(rows)?rows:[]).filter(x=>x?.guid&&!String(x.guid).startsWith('all-'));if(s&&!String(s.teamGuid||'').startsWith('all-'))src=real.find(x=>String(x.guid)===String(s.teamGuid))?.logoUrl||src;else src=real.find(x=>x.logoUrl)?.logoUrl||src}}catch{}if(src){const current=mark.querySelector('img')?.getAttribute('src')||'';if(current!==src){mark.classList.add('has-team-logo');mark.innerHTML=`<img src="${esc(src)}" alt="SV Argon">`}}else if(mark.classList.contains('has-team-logo')){mark.classList.remove('has-team-logo');mark.textContent='●'}}finally{headerBusy=false}}
async function renderPresence(force=false){const target=$('club-sub-presence');if(!target)return;if(!target.classList.contains('active')&&!force)return;if(presenceBusy)return;presenceBusy=true;try{const c=await context(force);if(!c){target.innerHTML='';target.dataset.personalPresence='logged-out';return}const teams=c.memberTeams;if(!teams.length){target.innerHTML='<div class="club-card-panel personal-presence-card"><h2>Aanwezigheid</h2><div class="club-empty">Je bent niet als speler aan een team gekoppeld.</div></div>';target.dataset.personalPresence=c.member.id;return}const today=new Date().toISOString().slice(0,10),byId=new Map();for(const tm of teams){if(!tm.foy_team_guid)continue;for(const m of await teamMatches(tm.foy_team_guid)){if(d(m.date)<today)continue;const h=guid(m,'home'),a=guid(m,'away');if(h!==String(tm.foy_team_guid)&&a!==String(tm.foy_team_guid))continue;byId.set(String(m.id),{m,tm})}}const rows=[...byId.values()].sort((a,b)=>`${d(a.m.date)}T${t(a.m.startTime)}`.localeCompare(`${d(b.m.date)}T${t(b.m.startTime)}`)),s=await client(),{data:attendance,error}=await s.from('attendance').select('foy_match_id,attending,driving').eq('member_id',c.member.id);if(error)throw error;const am=new Map((attendance||[]).map(x=>[String(x.foy_match_id),x]));target.innerHTML=`<div class="club-card-panel personal-presence-card"><h2>Aanwezigheid</h2><p>Kies per wedstrijd Ja of Nee. Dit is dezelfde aanwezigheid als in de wedstrijdinformatie.</p><div class="club-list">${rows.length?rows.map(({m,tm})=>{const v=am.get(String(m.id));return `<div class="club-row"><strong>${esc(label(m,'home'))} — ${esc(label(m,'away'))}</strong><small>${esc(shortDate(m.date))} · ${esc(t(m.startTime))} · ${esc(m.accommodationName||'')}</small><div class="attendance-choice"><button class="mini-button yes ${v?.attending===true?'selected':''}" data-personal-att="yes" data-match="${esc(m.id)}" data-team="${esc(tm.id)}">Ja</button><button class="mini-button no ${v?.attending===false?'selected':''}" data-personal-att="no" data-match="${esc(m.id)}" data-team="${esc(tm.id)}">Nee</button></div></div>`}).join(''):'<div class="club-empty">Geen komende wedstrijden voor jouw spelerteam(s).</div>'}</div></div>`;target.dataset.personalPresence=c.member.id;target.querySelectorAll('[data-personal-att]').forEach(b=>b.onclick=async()=>{const yes=b.dataset.personalAtt==='yes',old=am.get(String(b.dataset.match)),{error:e}=await s.rpc('set_match_attendance',{p_match_id:Number(b.dataset.match),p_team_id:b.dataset.team,p_attending:yes,p_driving:yes&&old?.driving===true});if(e)return toast(e.message);toast(yes?'Aanwezig: Ja':'Aanwezig: Nee');contextAt=0;await renderPresence(true);document.dispatchEvent(new CustomEvent('basketball-attendance-changed',{detail:{matchId:Number(b.dataset.match)}}))})}catch(e){console.warn('Persoonlijke aanwezigheid kon niet worden geladen',e);target.innerHTML='<div class="club-card-panel personal-presence-card"><h2>Aanwezigheid</h2><div class="club-empty">Aanwezigheid kon niet worden geladen.</div></div>'}finally{presenceBusy=false}}
function schedulePresence(force=false){if(presenceQueued)return;presenceQueued=true;setTimeout(()=>{presenceQueued=false;renderPresence(force)},80)}
function observe(){const obs=new MutationObserver(muts=>{let cardsChanged=false,presenceTouched=false;for(const m of muts){const el=m.target?.nodeType===1?m.target:m.target?.parentElement;if(el?.closest?.('#agendaList,#gamesList')||el?.id==='agendaList'||el?.id==='gamesList')cardsChanged=true;if(el?.closest?.('#club-sub-presence')||el?.id==='clubRoot')presenceTouched=true}if(cardsChanged)setTimeout(fixPlayerCounts,20);if(presenceTouched){const p=$('club-sub-presence');if(p?.classList.contains('active')&&!p.querySelector('.personal-presence-card'))schedulePresence(false)}});obs.observe(document.documentElement,{childList:true,subtree:true,attributes:true,attributeFilter:['class']})}

let trainingAdminTimer=0,trainingEditId='',trainingAdminBusy=false;
function injectTrainingAdminCss(){if($('trainingAdminRestoreCss'))return;const s=document.createElement('style');s.id='trainingAdminRestoreCss';s.textContent='#restoredTrainingAdmin{margin-bottom:20px;padding-bottom:18px;border-bottom:1px solid var(--line)}#restoredTrainingAdmin h3{margin:8px 0 10px}#restoredTrainingAdmin .restored-training-list{display:grid;gap:8px;margin-top:10px}#restoredTrainingAdmin .restored-training-row{border:1px solid var(--line);border-radius:14px;background:var(--card);padding:10px 11px}#restoredTrainingAdmin .restored-training-row strong{display:block;font-size:12px}#restoredTrainingAdmin .restored-training-row small{display:block;margin-top:3px;color:var(--muted);font-size:9px;line-height:1.35}';document.head.appendChild(s)}
async function renderTrainingAdmin(){const root=$('admin-agenda');if(!root||trainingAdminBusy)return;trainingAdminBusy=true;try{const c=await context(true);if(!c||c.member.role!=='admin')return;const s=await client(),[{data:teams,error:te},{data:rows,error:re}]=await Promise.all([s.from('teams').select('*').eq('active',true).order('team_name'),s.from('training_sessions').select('*').order('training_date').order('start_time')]);if(te)throw te;if(re)throw re;const list=rows||[],edit=list.find(x=>String(x.id)===String(trainingEditId));if(trainingEditId&&!edit)trainingEditId='';let panel=$('restoredTrainingAdmin');if(!panel){panel=document.createElement('div');panel.id='restoredTrainingAdmin';root.prepend(panel)}const teamOptions=(selected='')=>`<option value="">Alle teams</option>${(teams||[]).map(x=>`<option value="${esc(x.id)}" ${String(x.id)===String(selected)?'selected':''}>${esc(x.team_name)}</option>`).join('')}`;panel.innerHTML=`<h3>${edit?'Training wijzigen':'Training toevoegen'}</h3><form id="restoredTrainingForm" class="club-form"><input id="restoredTrainingId" type="hidden" value="${esc(edit?.id||'')}"><div class="club-form two"><label>Team<select id="restoredTrainingTeam">${teamOptions(edit?.team_id||'')}</select></label><label>Titel<input id="restoredTrainingTitle" value="${esc(edit?.title||'Training')}"></label><label>Datum<input id="restoredTrainingDate" type="date" required value="${esc(edit?.training_date||'')}"></label><label>Start<input id="restoredTrainingStart" type="time" required value="${esc(t(edit?.start_time||''))}"></label><label>Einde<input id="restoredTrainingEnd" type="time" value="${esc(t(edit?.end_time||''))}"></label><label>Locatie<input id="restoredTrainingLocation" value="${esc(edit?.location||'')}"></label></div><label>Opmerking<textarea id="restoredTrainingNotes">${esc(edit?.notes||'')}</textarea></label><div class="club-actions"><button class="primary-button" type="submit">Training opslaan</button>${edit?'<button class="mini-button" id="restoredTrainingCancel" type="button">Annuleren</button>':''}</div></form><h3>Trainingen</h3><div class="restored-training-list">${list.length?list.map(x=>{const tm=(teams||[]).find(y=>String(y.id)===String(x.team_id));return `<div class="restored-training-row"><strong>Training · ${esc(x.title||'Training')}</strong><small>${esc(shortDate(x.training_date))} · ${esc(t(x.start_time)||'—')}${x.end_time?' - '+esc(t(x.end_time)):''} · ${esc(tm?.team_name||'Hele vereniging')}${x.location?' · '+esc(x.location):''}</small>${x.notes?`<small>${esc(x.notes)}</small>`:''}<div class="mini-actions"><button class="mini-button" data-restored-training-edit="${esc(x.id)}">Wijzigen</button><button class="mini-button danger" data-restored-training-delete="${esc(x.id)}">Verwijderen</button></div></div>`}).join(''):'<div class="club-empty">Nog geen trainingen.</div>'}</div>`;panel.querySelector('#restoredTrainingForm').onsubmit=saveRestoredTraining;panel.querySelector('#restoredTrainingCancel')?.addEventListener('click',()=>{trainingEditId='';renderTrainingAdmin()});panel.querySelectorAll('[data-restored-training-edit]').forEach(b=>b.onclick=()=>{trainingEditId=String(b.dataset.restoredTrainingEdit);renderTrainingAdmin().then(()=>requestAnimationFrame(()=>$('restoredTrainingForm')?.scrollIntoView({behavior:'smooth',block:'start'})))});panel.querySelectorAll('[data-restored-training-delete]').forEach(b=>b.onclick=()=>deleteRestoredTraining(b.dataset.restoredTrainingDelete)}catch(e){console.warn('Trainingbeheer kon niet worden geladen',e);toast(e.message||'Trainingbeheer kon niet worden geladen.')}finally{trainingAdminBusy=false}}
async function saveRestoredTraining(e){e.preventDefault();try{const c=await context(true);if(!c||c.member.role!=='admin')return toast('Alleen admins mogen trainingen wijzigen.');const id=$('restoredTrainingId')?.value||'',p={team_id:$('restoredTrainingTeam')?.value||null,title:$('restoredTrainingTitle')?.value.trim()||'Training',training_date:$('restoredTrainingDate')?.value||'',start_time:$('restoredTrainingStart')?.value||'',end_time:$('restoredTrainingEnd')?.value||null,location:$('restoredTrainingLocation')?.value.trim()||null,notes:$('restoredTrainingNotes')?.value.trim()||null};if(!p.training_date||!p.start_time)return toast('Datum en starttijd zijn verplicht.');const s=await client();if(id){const {error}=await s.from('training_sessions').update(p).eq('id',id);if(error)throw error}else{const {error}=await s.from('training_sessions').insert({...p,created_by:c.member.id});if(error)throw error}trainingEditId='';contextCache=null;contextAt=0;toast('Training opgeslagen.');await renderTrainingAdmin()}catch(err){console.error(err);toast(err.message||'Training opslaan is mislukt.')}}
async function deleteRestoredTraining(id){if(!confirm('Training verwijderen?'))return;try{const c=await context(true);if(!c||c.member.role!=='admin')return toast('Alleen admins mogen trainingen verwijderen.');const s=await client(),{error}=await s.from('training_sessions').delete().eq('id',id);if(error)throw error;if(String(trainingEditId)===String(id))trainingEditId='';contextCache=null;contextAt=0;toast('Training verwijderd.');await renderTrainingAdmin()}catch(err){console.error(err);toast(err.message||'Training verwijderen is mislukt.')}}
function scheduleTrainingAdmin(){clearTimeout(trainingAdminTimer);trainingAdminTimer=setTimeout(()=>{const root=$('admin-agenda');if(root&&!$('restoredTrainingAdmin')){injectTrainingAdminCss();renderTrainingAdmin()}},90)}

function observeTrainingAdmin(){new MutationObserver(()=>scheduleTrainingAdmin()).observe(document.documentElement,{childList:true,subtree:true})}
document.addEventListener('click',e=>{if(e.target.closest('[data-clubview="presence"]'))schedulePresence(true);if(e.target.closest('[data-adminview="agenda"]'))scheduleTrainingAdmin()},true);
document.addEventListener('DOMContentLoaded',()=>{injectCss();fixPlayerCounts();applyHeaderLogo();schedulePresence(false);observe();injectTrainingAdminCss();scheduleTrainingAdmin();observeTrainingAdmin();$('teamSelect')?.addEventListener('change',()=>setTimeout(applyHeaderLogo,120));setInterval(()=>{fixPlayerCounts();const p=$('club-sub-presence');if(p?.classList.contains('active')&&!p.querySelector('.personal-presence-card'))schedulePresence(true)},1800)});
})();