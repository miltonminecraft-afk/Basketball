(()=>{
'use strict';
const SUPABASE_URL='https://elpnfmlrkoemjrnzaeok.supabase.co';
const SUPABASE_KEY='sb_publishable_GPzLwaKeevg3e8CNjw9oAQ_50NW2xlg';
let sb=null,teams=[],members=[],events=[],swapRows=[];
const state={team:'',person:'',editing:''};
let mountBusy=false,refreshBusy=false;
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
const t=v=>String(v||'').slice(0,5);
const norm=v=>String(v||'').trim().toLowerCase();
const roleLabel=r=>r==='referee'?'Scheidsrechter':r==='table'?'Tafel':'Overig';
function toast(msg){const el=$('toast');if(!el){alert(msg);return}el.textContent=msg;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2400)}
function dateLabel(v){try{return new Intl.DateTimeFormat('nl-NL',{weekday:'long',day:'2-digit',month:'long',year:'numeric'}).format(new Date(`${String(v).slice(0,10)}T12:00:00`)).toUpperCase()}catch{return String(v||'').toUpperCase()}}
async function client(){if(sb)return sb;const mod=await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm');sb=mod.createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{persistSession:true,detectSessionInUrl:true,autoRefreshToken:true}});return sb}
async function secureLogin(form){
 const email=String(form.querySelector('#loginEmail')?.value||'').trim();if(!email)return;
 const button=form.querySelector('button[type="submit"]');if(button){button.disabled=true;button.textContent='Controleren…'}
 try{
  const s=await client(),{data:allowed,error:checkError}=await s.rpc('member_login_allowed',{p_email:email,p_phone:null});
  if(checkError)throw checkError;
  if(!allowed){toast('Dit e-mailadres staat niet als actief lid geregistreerd.');return}
  if(button)button.textContent='Inloglink versturen…';
  const {error}=await s.auth.signInWithOtp({email,options:{shouldCreateUser:true,emailRedirectTo:location.origin+location.pathname}});if(error)throw error;
  toast('Inloglink verstuurd. Controleer je e-mail.');
 }catch(e){console.error(e);toast(e.message||'Inloggen is mislukt.')}finally{if(button){button.disabled=false;button.textContent='Stuur inloglink'}}
}
function injectCss(){if($('adminAgendaV2Css'))return;const s=document.createElement('style');s.id='adminAgendaV2Css';s.textContent=`
#admin-agenda[data-admin-agenda-v2="1"]{display:block}
.admin-agenda-v2-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;margin:4px 0 12px}
.admin-agenda-v2-head h3{margin:0;font-size:18px}.admin-agenda-v2-head p{margin:3px 0 0;color:var(--muted);font-size:11px;line-height:1.35}
.admin-agenda-filters{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:14px}
.admin-agenda-filters label{font-size:10px;font-weight:800;color:var(--muted)}
.admin-agenda-filters select{margin-top:4px;width:100%;min-height:40px;border:1px solid var(--line);border-radius:12px;background:var(--card);padding:0 10px;font:inherit;font-size:12px;font-weight:700;color:var(--ink)}
.admin-agenda-day{margin:16px 0 8px;font-size:11px;font-weight:900;letter-spacing:.02em;color:var(--ink)}
.admin-agenda-event{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:10px 11px;margin-bottom:8px}
.admin-agenda-event-top{display:grid;grid-template-columns:52px minmax(0,1fr) auto;gap:9px;align-items:start}
.admin-agenda-time{font-size:17px;font-weight:900;line-height:1.05}.admin-agenda-time small{display:block;margin-top:4px;font-size:8px;color:var(--muted);font-weight:800;line-height:1.2}
.admin-agenda-main{min-width:0}.admin-agenda-title{font-size:12px;font-weight:900;line-height:1.25}.admin-agenda-meta{margin-top:3px;font-size:9px;color:var(--muted);line-height:1.3}
.admin-task-chips{display:flex;flex-wrap:wrap;gap:4px;margin-top:7px}.admin-task-chip{display:inline-flex;align-items:center;gap:3px;padding:4px 6px;border-radius:9px;background:#f1f2f7;font-size:9px;line-height:1.1}.admin-task-chip strong{font-weight:900}.admin-task-chip.swap{background:#fff1e8;color:#a64700}
.admin-agenda-edit{border-top:1px solid var(--line);margin-top:10px;padding-top:10px}.admin-edit-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px}.admin-edit-grid label{font-size:9px;font-weight:800;color:var(--muted)}.admin-edit-grid input,.admin-edit-grid select{width:100%;margin-top:3px;min-height:38px;border:1px solid var(--line);border-radius:10px;background:var(--card);padding:0 9px;font:inherit;font-size:11px;color:var(--ink)}
.admin-assignment-edit{display:grid;grid-template-columns:minmax(0,1.3fr) minmax(0,.9fr) auto;gap:6px;align-items:center;margin-top:7px}.admin-assignment-edit select{width:100%;min-height:36px;border:1px solid var(--line);border-radius:10px;background:var(--card);padding:0 7px;font:inherit;font-size:10px;color:var(--ink)}
.admin-edit-actions{display:flex;flex-wrap:wrap;gap:6px;margin-top:9px}.admin-edit-actions .mini-button{margin:0}
.admin-swap-note{font-size:8px;font-weight:900;color:#b44a00;margin-top:3px}
@media(max-width:520px){.admin-agenda-filters{grid-template-columns:1fr 1fr}.admin-agenda-event-top{grid-template-columns:46px minmax(0,1fr) auto}.admin-assignment-edit{grid-template-columns:1fr 1fr}.admin-assignment-edit .admin-assignment-actions{grid-column:1/-1}.admin-edit-grid{grid-template-columns:1fr 1fr}}
`;document.head.appendChild(s)}
async function load(){const s=await client();const [{data:{session}},{data:ts,error:te},{data:ms,error:me},{data:es,error:ee},{data:sw,error:se}]=await Promise.all([
 s.auth.getSession(),
 s.from('teams').select('*').eq('active',true).order('team_name'),
 s.from('members').select('*').eq('active',true).order('full_name'),
 s.from('task_events').select('*,task_assignments(*)').eq('active',true).order('event_date').order('start_time'),
 s.from('task_swap_requests').select('*').order('created_at',{ascending:false}).limit(500)
]);
 if(!session)throw new Error('Niet ingelogd.');
 const {data:meNow,error:whoErr}=await s.rpc('sync_current_member');if(whoErr)throw whoErr;if(meNow?.role!=='admin')throw new Error('Alleen admins hebben toegang tot Beheeragenda.');
 if(te)throw te;if(me)throw me;if(ee)throw ee;if(se)console.warn(se);
 teams=ts||[];members=ms||[];events=es||[];swapRows=sw||[];
}
function teamOptions(selected=''){return `<option value="">Alle teams</option>${teams.map(x=>`<option value="${esc(x.id)}" ${String(selected)===String(x.id)?'selected':''}>${esc(x.team_name)}</option>`).join('')}`}
function memberFilterOptions(selected=''){return `<option value="">Alle personen</option>${members.map(x=>`<option value="${esc(x.id)}" ${String(selected)===String(x.id)?'selected':''}>${esc(x.full_name||x.email||'Lid')}</option>`).join('')}`}
function memberEditOptions(selected=''){return `<option value="">Niet toegewezen</option>${members.map(x=>`<option value="${esc(x.id)}" ${String(selected)===String(x.id)?'selected':''}>${esc(x.full_name||x.email||'Lid')}</option>`).join('')}`}
function swapFor(a){return swapRows.find(s=>String(s.assignment_id)===String(a.id))||null}
function passes(e){if(state.team&&String(e.team_id||'')!==String(state.team))return false;if(state.person){const m=members.find(x=>String(x.id)===String(state.person));const ok=(e.task_assignments||[]).some(a=>String(a.assigned_member_id||'')===String(state.person)||(!a.assigned_member_id&&norm(a.assigned_name)===norm(m?.full_name)));if(!ok)return false}return true}
function taskChips(e){const as=e.task_assignments||[];if(!as.length)return '<span class="admin-task-chip"><strong>Geen taken</strong></span>';return as.map(a=>{const sw=swapFor(a);const swapped=sw&&sw.status!=='cancelled';const tag=swapped?(sw.status==='open'?' · wissel open':' · gewisseld'):'';return `<span class="admin-task-chip ${swapped?'swap':''}"><strong>${esc(roleLabel(a.role))}:</strong> ${esc(a.assigned_name||'Niet toegewezen')}${esc(tag)}</span>`}).join('')}
function editBlock(e){const as=e.task_assignments||[];return `<div class="admin-agenda-edit" data-edit-block="${esc(e.id)}"><div class="admin-edit-grid"><label>Team<select data-event-field="team_id">${teamOptions(e.team_id||'')}</select></label><label>Datum<input data-event-field="event_date" type="date" value="${esc(e.event_date||'')}"></label><label>Aanwezig-tijd<input data-event-field="arrival_time" type="time" value="${esc(t(e.arrival_time))}"></label><label>Start wedstrijd<input data-event-field="start_time" type="time" value="${esc(t(e.start_time))}"></label><label>Locatie<input data-event-field="location" value="${esc(e.location||'')}"></label><label>Veld<input data-event-field="field" value="${esc(e.field||'')}"></label></div><div class="admin-edit-actions"><button class="mini-button primary" data-save-event="${esc(e.id)}">Tijden en gegevens opslaan</button><button class="mini-button" data-close-event="${esc(e.id)}">Sluiten</button></div><div style="margin-top:10px;font-size:10px;font-weight:900">Taken</div>${as.map(a=>assignmentEditor(a)).join('')}<div class="admin-edit-actions"><button class="mini-button" data-add-assignment="${esc(e.id)}">+ Toewijzing</button><button class="mini-button danger" data-delete-event="${esc(e.id)}">Taakmoment verwijderen</button></div></div>`}
function assignmentEditor(a){const sw=swapFor(a),note=sw?(sw.status==='open'?'Wissel staat open':sw.status==='cancelled'?'':'Taak is via wissel aangepast'):'';return `<div class="admin-assignment-edit" data-assignment="${esc(a.id)}"><select data-assignment-member>${memberEditOptions(a.assigned_member_id||'')}</select><select data-assignment-role><option value="referee" ${a.role==='referee'?'selected':''}>Scheidsrechter</option><option value="table" ${a.role==='table'?'selected':''}>Tafel</option><option value="other" ${a.role==='other'?'selected':''}>Overig</option></select><div class="admin-assignment-actions"><button class="mini-button" data-save-assignment="${esc(a.id)}">Opslaan</button><button class="mini-button danger" data-delete-assignment="${esc(a.id)}">×</button></div>${note?`<div class="admin-swap-note">${esc(note)}</div>`:''}</div>`}
function eventCard(e){const team=teams.find(x=>String(x.id)===String(e.team_id));const location=[e.location,e.field].filter(Boolean).join(' · ');return `<div class="admin-agenda-event" data-event="${esc(e.id)}"><div class="admin-agenda-event-top"><div class="admin-agenda-time">${esc(t(e.start_time)||'—')}<small>${e.arrival_time?`aanw. ${esc(t(e.arrival_time))}`:'geen aankomsttijd'}</small></div><div class="admin-agenda-main"><div class="admin-agenda-title">${esc(e.home||'')} — ${esc(e.away||'')}</div><div class="admin-agenda-meta">${esc(team?.team_name||'Geen team')}${location?' · '+esc(location):''}</div><div class="admin-task-chips">${taskChips(e)}</div></div><button class="mini-button" data-edit-event="${esc(e.id)}">Wijzigen</button></div>${String(state.editing)===String(e.id)?editBlock(e):''}</div>`}
function render(){const root=$('admin-agenda');if(!root)return;root.dataset.adminAgendaV2='1';const rows=events.filter(passes).sort((a,b)=>`${a.event_date}T${a.start_time||''}`.localeCompare(`${b.event_date}T${b.start_time||''}`));let body='',day='';for(const e of rows){if(e.event_date!==day){day=e.event_date;body+=`<div class="admin-agenda-day">${esc(dateLabel(day))}</div>`}body+=eventCard(e)}root.innerHTML=`<div class="admin-agenda-v2-head"><div><h3>Beheeragenda</h3><p>Taken, toewijzingen en lokale wedstrijdtijden beheren.</p></div></div><div class="admin-agenda-filters"><label>Team<select id="adminAgendaTeamFilter">${teamOptions(state.team)}</select></label><label>Persoon<select id="adminAgendaPersonFilter">${memberFilterOptions(state.person)}</select></label></div><div id="adminAgendaV2List">${body||'<div class="club-empty">Geen taakmomenten voor deze filters.</div>'}</div>`;bind()}
function bind(){const root=$('admin-agenda');if(!root)return;const tf=$('adminAgendaTeamFilter'),pf=$('adminAgendaPersonFilter');if(tf)tf.onchange=()=>{state.team=tf.value;state.editing='';render()};if(pf)pf.onchange=()=>{state.person=pf.value;state.editing='';render()};root.querySelectorAll('[data-edit-event]').forEach(b=>b.onclick=()=>{state.editing=String(b.dataset.editEvent);render();requestAnimationFrame(()=>root.querySelector(`[data-event="${CSS.escape(state.editing)}"]`)?.scrollIntoView({behavior:'smooth',block:'nearest'}))});root.querySelectorAll('[data-close-event]').forEach(b=>b.onclick=()=>{state.editing='';render()});root.querySelectorAll('[data-save-event]').forEach(b=>b.onclick=()=>saveEvent(b.dataset.saveEvent));root.querySelectorAll('[data-save-assignment]').forEach(b=>b.onclick=()=>saveAssignment(b.dataset.saveAssignment));root.querySelectorAll('[data-delete-assignment]').forEach(b=>b.onclick=()=>deleteAssignment(b.dataset.deleteAssignment));root.querySelectorAll('[data-add-assignment]').forEach(b=>b.onclick=()=>addAssignment(b.dataset.addAssignment));root.querySelectorAll('[data-delete-event]').forEach(b=>b.onclick=()=>deleteEvent(b.dataset.deleteEvent))}
async function refresh(keepEdit=true){if(refreshBusy)return;refreshBusy=true;try{const edit=state.editing;await load();if(keepEdit)state.editing=edit;render()}catch(e){console.error(e);toast(e.message||'Beheeragenda kon niet worden geladen.')}finally{refreshBusy=false}}
async function saveEvent(id){const block=document.querySelector(`[data-edit-block="${CSS.escape(String(id))}"]`);if(!block)return;const val=k=>block.querySelector(`[data-event-field="${k}"]`)?.value||'';const payload={team_id:val('team_id')||null,event_date:val('event_date'),arrival_time:val('arrival_time')||null,start_time:val('start_time'),location:val('location').trim()||null,field:val('field').trim()||null};if(!payload.event_date||!payload.start_time)return toast('Datum en starttijd zijn verplicht.');const s=await client(),{error}=await s.from('task_events').update(payload).eq('id',id);if(error)return toast(error.message);toast('Wedstrijdtijden opgeslagen.');await refresh(true)}
async function saveAssignment(id){const row=document.querySelector(`[data-assignment="${CSS.escape(String(id))}"]`);if(!row)return;const memberId=row.querySelector('[data-assignment-member]')?.value||null,role=row.querySelector('[data-assignment-role]')?.value||'other',m=members.find(x=>String(x.id)===String(memberId));const s=await client(),{error}=await s.from('task_assignments').update({assigned_member_id:memberId,assigned_name:m?.full_name||'Niet toegewezen',role}).eq('id',id);if(error)return toast(error.message);toast('Taak opgeslagen.');await refresh(true)}
async function deleteAssignment(id){if(!confirm('Deze toewijzing verwijderen?'))return;const s=await client(),{error}=await s.from('task_assignments').delete().eq('id',id);if(error)return toast(error.message);await refresh(true)}
async function addAssignment(eventId){const e=events.find(x=>String(x.id)===String(eventId)),slot=Math.max(0,...(e?.task_assignments||[]).map(x=>Number(x.slot)||0))+1,s=await client(),{error}=await s.from('task_assignments').insert({task_event_id:eventId,assigned_member_id:null,assigned_name:'Niet toegewezen',role:'other',slot});if(error)return toast(error.message);await refresh(true)}
async function deleteEvent(id){if(!confirm('Dit taakmoment en alle toewijzingen verwijderen?'))return;const s=await client(),{error}=await s.from('task_events').delete().eq('id',id);if(error)return toast(error.message);state.editing='';await refresh(false)}
function cleanupLegacyAdmin(){
 const first=$('firstAdminBox');if(first)first.remove();
 const training=$('trainingForm');if(training&&training.closest('#admin-agenda')){const h=training.previousElementSibling;if(h&&h.tagName==='H3'&&/training/i.test(h.textContent||''))h.remove();training.remove()}
 const task=$('taskEventForm');if(task&&task.closest('#admin-agenda')){const h=task.previousElementSibling;if(h&&h.tagName==='H3'&&/taak\s*\/\s*wedstrijd toevoegen/i.test(h.textContent||''))h.remove();task.remove()}
 document.querySelectorAll('#admin-members [data-edit-member]').forEach(edit=>{const id=String(edit.dataset.editMember||'');if(!id||edit.parentElement?.querySelector(`[data-admin-delete-member="${CSS.escape(id)}"]`))return;const card=edit.closest('.club-row'),name=String(card?.querySelector('strong')?.textContent||'dit lid').trim();const del=document.createElement('button');del.type='button';del.className='mini-button danger';del.dataset.adminDeleteMember=id;del.dataset.memberName=name;del.textContent='Verwijderen';edit.parentElement?.appendChild(del)});
 const root=$('clubRoot'),error=root?.querySelector('.error-box');if(error&&!$('blockedMemberLogout')&&/clublid|actief/i.test(error.textContent||'')){const btn=document.createElement('button');btn.id='blockedMemberLogout';btn.type='button';btn.className='mini-button';btn.textContent='Uitloggen';btn.style.marginTop='12px';btn.onclick=async()=>{const s=await client();await s.auth.signOut();location.reload()};error.appendChild(btn)}
}
async function deleteMember(button){const id=String(button.dataset.adminDeleteMember||''),name=String(button.dataset.memberName||'dit lid');if(!id||!confirm(`Weet je zeker dat je ${name} wilt verwijderen?`))return;button.disabled=true;try{const s=await client(),{error}=await s.rpc('admin_delete_member',{p_member_id:id});if(error)throw error;toast('Lid verwijderd.');setTimeout(()=>location.reload(),350)}catch(e){console.error(e);toast(e.message||'Lid verwijderen is mislukt.');button.disabled=false}}
async function mount(){const root=$('admin-agenda');if(!root||root.dataset.adminAgendaV2==='1'||mountBusy)return;mountBusy=true;try{injectCss();root.dataset.adminAgendaV2='loading';root.innerHTML='<div class="club-empty">Beheeragenda laden…</div>';await load();render()}catch(e){console.error(e);root.dataset.adminAgendaV2='error';root.innerHTML=`<div class="club-empty">${esc(e.message||'Beheeragenda kon niet worden geladen.')}</div>`}finally{mountBusy=false}}
let timer=0;function schedule(){cleanupLegacyAdmin();clearTimeout(timer);timer=setTimeout(mount,60)}
document.addEventListener('submit',e=>{const form=e.target;if(!(form instanceof HTMLFormElement)||form.id!=='loginForm')return;e.preventDefault();e.stopImmediatePropagation();secureLogin(form)},true);
document.addEventListener('click',e=>{const b=e.target.closest?.('[data-admin-delete-member]');if(!b)return;e.preventDefault();deleteMember(b)},true);
new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true});
document.addEventListener('DOMContentLoaded',schedule);
})();